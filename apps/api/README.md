# @open-music/api

Backend API Open Music (NestJS). Конфигурация и модули наполняются в задачах 3.x плана.

## Запуск (dev)

Перед запуском поднимите инфраструктуру:

```bash
pnpm infra:up
```

Затем стартуйте API:

```bash
pnpm --filter @open-music/api dev
```
